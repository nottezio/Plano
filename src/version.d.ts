// Ambient declaration so the strict TS program gets exact literal-ish types
// from the plain-JS single source of truth (SPEC 16).
export const APP_VERSION: string;
export const CACHE_NAME: string;
export const CACHE_PREFIX: string;
